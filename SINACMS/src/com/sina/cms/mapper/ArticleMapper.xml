<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sina.cms.dao.ArticleDao">

	<insert id="addArticle" parameterType="ArticleModel">
		insert into article (
			title, originUrl, clickNums,replyNums,createDate,updateDate,
			publishDate,status,ownerUsername,content)
		values (
			#{title}, #{originUrl}, #{clickNums},#{replyNums},#{createDate},#{updateDate},
			#{publishDate},#{status},#{ownerUsername},#{content}
		)
	</insert>
	
</mapper>